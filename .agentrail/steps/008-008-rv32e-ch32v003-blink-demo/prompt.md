Add an RV32E board blink path using a CH32V003-style board TOML. Create a CH32V003 board placeholder with arch/profile rv32ec, a generic GPIO LED alias, and generic UART metadata if appropriate. Update board_blink or add a Rust example/test so both ESP32-C3 and the RV32E board can run the shared blink demo through emulator Machine with profile derived from board TOML. Document exactly which Rust examples are runnable and note that assembler-source blink still needs load-address/pseudo-op support if it is not completed in this step.