Connect the generic GPIO target model to emulator-facing MMIO. Define a small MMIO bus/device abstraction in the appropriate crate boundary, add a generic GPIO device that records writes as pin transitions, and add an emulator or target integration test showing a blink-like program can toggle the board led alias through MMIO without ESP-IDF or vendor SDKs. Keep board register accuracy out of scope; use the existing generic-gpio MMIO kind and board TOML base address.