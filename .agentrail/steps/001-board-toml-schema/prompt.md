Scaffold sw-rv32i-target as a Rust crate for board metadata. Add a data-driven board TOML schema with fields for id, family, arch/profile, memory map, GPIO pins, aliases such as led, and MMIO device declarations. Implement loading/parsing without requiring vendor SDKs, add ESP32-C3/C5/C6 placeholder board files, and add tests proving boards load and that demos can bind to logical aliases so blink can be shared across boards using the same GPIO pin. Document the schema and next blink/MMIO steps.