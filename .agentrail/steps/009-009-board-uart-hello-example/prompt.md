Add a runnable Rust example for board UART text output. The example should load a board TOML file, derive the profile from arch, build or assemble a hello-style program that writes bytes through uart0 generic-uart MMIO, run it through emulator Machine, and print the captured UART output. Prefer assembler-source support if full 32-bit MMIO address loading is available; otherwise use ISA-helper encoding and clearly document the assembler gap.